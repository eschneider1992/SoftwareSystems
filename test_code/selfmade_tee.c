#include <stdio.h>

int main(int argc, char *argv[]) {

	if (argv[1] == '-a')
		FILE 

	// while ((ch = getopt(argc, argv, "a")) != EOF) {
	// 	switch(ch) {
	// 		case 'a':
	// 			FILE *out = fopen(argv)


	// 	}
	// }
}